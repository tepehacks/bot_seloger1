Analyse tout le projet et corrige automatiquement tous les problèmes visibles dans les logs.

PROBLÈME N°1 : Logging cassé

Erreur répétée :

PermissionError: [WinError 32]

Le fichier :

logs/bot_seloger.log

est verrouillé pendant le rollover.

Le logger tente de renommer :

bot_seloger.log
→ bot_seloger.log.2026-06-03

mais le fichier est encore utilisé.

Je veux :

* identifier pourquoi plusieurs handlers utilisent le même fichier
* éviter les handlers dupliqués
* éviter plusieurs initialisations du logger
* remplacer TimedRotatingFileHandler si nécessaire
* utiliser delay=True
* fermer correctement les handlers
* empêcher tout crash du logging

Le logger ne doit jamais interrompre le programme.

━━━━━━━━━━━━━━━━━━━━━━

PROBLÈME N°2 : Recherche de ville extrêmement lente

Le log montre :

Temps recherche : 104.1s

La résolution de ville prend plus d'une minute.

Je veux :

* analyser city_resolver.py
* supprimer tous les sleep inutiles
* remplacer les sleep par WebDriverWait
* réduire les timeouts
* optimiser la récupération du code ville
* mettre un cache mémoire :

Marseille -> AD08FR4491

afin d'éviter de refaire la recherche plusieurs fois

* ajouter un cache disque JSON

Objectif :
moins de 5 secondes pour obtenir le code ville.

━━━━━━━━━━━━━━━━━━━━━━

PROBLÈME N°3 : Optimisation Selenium

Je veux :

* désactiver les images
* désactiver les notifications
* désactiver les animations
* utiliser page_load_strategy='eager'
* réduire les temps de chargement
* supprimer les waits inutiles

━━━━━━━━━━━━━━━━━━━━━━

PROBLÈME N°4 : Robustesse

Créer :

safe_find_element()
safe_click()
safe_send_keys()

avec retry automatique.

Ne jamais faire planter le programme sur :

* NoSuchElementException
* TimeoutException
* StaleElementReferenceException
* ElementClickInterceptedException
* InvalidSelectorException

Passer automatiquement à l'annonce suivante.

━━━━━━━━━━━━━━━━━━━━━━

PROBLÈME N°5 : Diagnostic formulaire

Ajouter un mode debug qui affiche :

* tous les inputs
* name
* id
* placeholder
* type
* visible
* enabled

avant remplissage.

Sauvegarder automatiquement :

screenshots/debug_formulaire.png

et

debug/formulaire.html

si le remplissage échoue.

━━━━━━━━━━━━━━━━━━━━━━

PROBLÈME N°6 : Rapport final

Afficher :

* temps de recherche
* temps de scraping
* temps de contact
* annonces trouvées
* annonces contactées
* annonces échouées

et les erreurs rencontrées.

Je veux le code complet corrigé pour :

* logger_config.py
* city_resolver.py
* scraper.py
* contact_manager.py
* main.py

avec priorité maximale sur la vitesse et la stabilité.
